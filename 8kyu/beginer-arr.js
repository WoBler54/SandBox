// Given an array of integers, return a new array with each value doubled.

// For example:

function maps(x){
    let result = [];
     for(let num of x){
       result.push(num * 2);
     }
     return result;
   }